{
  "name": "zemi-doc-hero",
  "version": "1.0.0",
  "description": "Monorepo of Document Hero",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "lint-staged": {
    "apps/api/src/**/*.{ts,js}": [
      "npm run eslint:fix",
      "npm run format"
    ],
    "apps/api/src/**/*.{json,md}": [
      "npm run format"
    ]
  },
  "author": "jsmr04@gmail.com",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7",
    "prettier": "^3.6.2"
  }
}
